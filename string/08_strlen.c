#include<stdio.h>
#include<string.h>
int main()
{
     char *a="Wiry Vikash ";//space is counted as lenght
     int len=strlen(a);
     printf("the value of length is %d",len);
     return 0;
}