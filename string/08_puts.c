#include<stdio.h>
int main()
{
     char c[36];
     printf("enter the name \n");
     gets(c);//it will gives multiple string
     puts(c);//used as the printf statement
    //  printf("YOur name is %s",c);
     
     return 0;
}